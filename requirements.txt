# ARSO Ontology AAS Generation — Python dependencies
# Covers the FastAPI backend (api/) and the generation/transformation/validation pipeline.
# Install from the repo root:  pip install -r requirements.txt

# --- Web API ---
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0
httpx>=0.27

# --- Config / serialization ---
pyyaml

# --- RDF + SHACL validation ---
rdflib>=7.0.0
pyshacl>=0.27.0
basyx-python-sdk>=1.2.1,<2.0.0

# --- LLM providers ---
openai>=1.40.0            # Groq (OpenAI-compatible API)
google-genai>=0.7.0       # Gemini

# --- PDF / document parsing ---
pymupdf4llm>=0.0.17       # pulls in PyMuPDF (imported as `fitz`)
pdfplumber>=0.11

# --- Testing extras (optional; only needed for Testing/ plot scripts) ---
# matplotlib>=3.8
